/** import { VehiculeFactory } from "./vehiculeFactory";

export class Catalog extends VehiculeFactory {

    public catalog(): Array<VehiculeFactory> {
        const catalog: Catalog = new Catalog();
        catalog.build(): void {
            
        }
    }

    public build(): void {
        }

    public marge(): void {
        const marge = this.sellPrice - this.costPrice;
        console.log('Grâce à vous, nous allons gagner ' + marge + ' euros')
        }

    public margeTotale(): void {
        const margeTotale = margeTotale + this.marge;
    }
}*/